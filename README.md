Must execute with command line argument 
`dia` (for data on diastolic blood pressure),
`sys` (for data on systolic blood pressure),
`eda` (for data on electrodermal activity),
`res` (for respiration data),
or `all` (for all data).